fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.12
pyyaml==6.0.2
aiofiles==24.1.0
httpx==0.28.0          # für FastAPI TestClient
pytest==8.3.4
pytest-asyncio==0.24.0

# Extraktion
pypdf>=4.0
pypdfium2>=4.30        # PDF-Text/Rendering, Ersatz für PyMuPDF (AGPL-3.0, für kommerziellen
                       # Vertrieb closed-source nicht ohne Zusatzlizenz nutzbar) -- Apache-2.0/BSD
pytesseract>=0.3.10    # OCR-Bridge zu Tesseract (Apache-2.0), rendert über pypdfium2
python-docx>=1.1
openpyxl>=3.1
striprtf>=0.0.26
cryptography>=42.0     # AES-verschlüsselte PDFs (pypdf braucht es, sonst „AES algorithm"-Fehler)

# KI-Suche
numpy>=1.26

# Foto-Browser (Galerie, Thumbnails)
pillow>=11.1
pillow-heif>=0.19

# System
psutil>=5.9
packaging>=23.0        # Versionsvergleich für den Server-Auto-Updater
requests>=2.31         # Menubar-Apps, Helper, Scheduler-Aufrufe an den eigenen Server
zeroconf>=0.130        # mDNS: Server meldet sich an, Helper findet ihn (auch nach Umzug)
